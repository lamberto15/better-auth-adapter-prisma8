---
"better-auth-adapter-prisma8": minor
---

The optional `typescript` peer dependency now requires TypeScript 6 (`^6.0.0`, was `^5.6.0`). It is only used to merge new models into an existing `contract.ts`; PSL contracts are unaffected.
